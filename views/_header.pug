
header.header
  nav.nav--retreats
     nav.nav.nav--user
      if user
        a.nav__el.nav__el--logout Log out
        a.nav__el(href='/me')
      else
        a.nav__el(href='/login') Log in


  .header__text-box
    h1.heading-primary
      span.heading-primary--main Breathe
      span.heading-primary--sub Drift off to paradise
      a.btn.btn--white.btn--animated(href='#section-retreats') All retreats
